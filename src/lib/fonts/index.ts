import { Inter_Tight, Unbounded, Mr_Dafoe } from 'next/font/google';

export const inter_tight = Inter_Tight({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter-tight',
});

export const unbounded = Unbounded({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-unbounded',
});

export const mr_dafoe = Mr_Dafoe({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-mr_dafoe',
});
